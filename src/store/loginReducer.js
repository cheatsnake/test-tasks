const initialState = {
    user: {
        login: 'developer21',
        password: '123456'
    }
}

export default function loginReducer(state = initialState, action) {
    switch (action.type) {
        default:
            break;
    }
    return state;
}